def adad_aval(a,b) :
    if a > b :
        c = b
        b = a
        a = c
    total = 0
    for i in range(a,b+1):
        flag = 0 
        for j in range(2,i):
            if i % j == 0 :
                flag+= 1
        if flag == 0 :
            total +=i
    return [total]
print(adad_aval(5,19))
